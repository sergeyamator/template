'use strict';

module.exports = () => {
  $.gulp.task('img-min', () => {
    return $.gulp.src('dev/assets/img/**/*')
      .pipe(imagemin({
        progressive: true,
        svgPlugins: [{removeViewBox: false}],
        use: [pngquant()]
      }))
      .pipe($.gulp.dest('prod/img'));
  });
};